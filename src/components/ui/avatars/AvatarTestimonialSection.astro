---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

const { src, alt } = Astro.props;

interface Props {
  src: string | ImageMetadata;
  alt: string;
}
---

<div class="shrink-0">
  {
    typeof src === 'string' ? (
      <Image
        class="size-8 rounded-full sm:h-[2.875rem] sm:w-[2.875rem]"
        src={src}
        alt={alt}
        inferSize
        loading="lazy"
        widths={[32, 46, 64]}
        sizes="(max-width: 640px) 32px, 46px"
        format="webp"
      />
    ) : (
      <Image
        class="size-8 rounded-full sm:h-[2.875rem] sm:w-[2.875rem]"
        src={src}
        alt={alt}
        loading="lazy"
        widths={[32, 46, 64]}
        sizes="(max-width: 640px) 32px, 46px"
        format="webp"
      />
    )
  }
</div>
